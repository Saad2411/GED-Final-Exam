using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnowflakePool : MonoBehaviour
{
    //Create pool of snowflakes that adds a snowflake when its instantiated
    //When the snowflake touches megaman or the ground, the snowflake will deactivate and gets added to the pool
    //Once a snowflake is deactivated and is instantiated back, its position will change to one of the spawn locations to fall again
    //The cycle then repeats
}
